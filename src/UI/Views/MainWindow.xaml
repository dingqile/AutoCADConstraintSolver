<Window x:Class="AutoCADConstraintSolver.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AutoCADConstraintSolver.UI.Controls"
        mc:Ignorable="d"
        Title="AutoCAD Constraint Solver" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button x:Name="BtnLine" Content="Line" Click="BtnLine_Click" Padding="10,5"/>
                <Button x:Name="BtnCircle" Content="Circle" Click="BtnCircle_Click" Padding="10,5"/>
                <Button x:Name="BtnArc" Content="Arc" Click="BtnArc_Click" Padding="10,5"/>
                <Separator/>
                <Button x:Name="BtnHorizontal" Content="Horizontal" Click="BtnHorizontal_Click" Padding="10,5"/>
                <Button x:Name="BtnVertical" Content="Vertical" Click="BtnVertical_Click" Padding="10,5"/>
                <Button x:Name="BtnParallel" Content="Parallel" Click="BtnParallel_Click" Padding="10,5"/>
                <Button x:Name="BtnPerpendicular" Content="Perpendicular" Click="BtnPerpendicular_Click" Padding="10,5"/>
                <Button x:Name="BtnCoincident" Content="Coincident" Click="BtnCoincident_Click" Padding="10,5"/>
                <Button x:Name="BtnDistance" Content="Distance" Click="BtnDistance_Click" Padding="10,5"/>
                <Button x:Name="BtnTangent" Content="Tangent" Click="BtnTangent_Click" Padding="10,5"/>
                <Separator/>
                <Button x:Name="BtnSolve" Content="Solve" Click="BtnSolve_Click" Padding="15,5" Background="LightGreen"/>
                <Button x:Name="BtnClear" Content="Clear" Click="BtnClear_Click" Padding="10,5"/>
                <Separator/>
                <Button x:Name="BtnRectangle" Content="Rectangle" Click="BtnRectangle_Click" Padding="10,5"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Canvas -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <controls:SketchCanvas x:Name="SketchCanvas"/>
            </Border>

            <!-- Properties Panel -->
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Properties" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        
                        <!-- Entity Properties -->
                        <GroupBox Header="Selected Entity" Margin="0,0,0,10">
                            <StackPanel x:Name="EntityPanel" Visibility="Collapsed">
                                <TextBlock x:Name="EntityTypeText" Margin="0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="X:" Margin="0,2"/>
                                    <TextBox x:Name="EntityX" Grid.Row="0" Grid.Column="1" Margin="2"/>
                                    <TextBlock Grid.Row="1" Text="Y:" Margin="0,2"/>
                                    <TextBox x:Name="EntityY" Grid.Row="1" Grid.Column="1" Margin="2"/>
                                    <TextBlock Grid.Row="2" Text="Z:" Margin="0,2"/>
                                    <TextBox x:Name="EntityZ" Grid.Row="2" Grid.Column="1" Margin="2"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <!-- Constraint Properties -->
                        <GroupBox Header="Constraints" Margin="0,0,0,10">
                            <ListBox x:Name="ConstraintsList" Height="150">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </GroupBox>

                        <!-- Statistics -->
                        <GroupBox Header="Statistics">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="Entities:" Margin="0,2"/>
                                    <TextBlock x:Name="EntityCount" Grid.Row="0" Grid.Column="1" Margin="5,2"/>
                                    <TextBlock Grid.Row="1" Text="Constraints:" Margin="0,2"/>
                                    <TextBlock x:Name="ConstraintCount" Grid.Row="1" Grid.Column="1" Margin="5,2"/>
                                    <TextBlock Grid.Row="2" Text="DOF:" Margin="0,2"/>
                                    <TextBlock x:Name="DOFText" Grid.Row="2" Grid.Column="1" Margin="5,2"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="SolverStatusText" Text="Not Solved"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
